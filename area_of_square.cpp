#include <iostream>
using namespace std;

int main()
{
    float length, area;
    cout << "Enter the length " << endl;
    cin >> length;
    area = length * length;
    cout << "Area is : " << area << endl;
    return 0;
}